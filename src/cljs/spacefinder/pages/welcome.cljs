(ns spacefinder.pages.welcome
  (:require
   [re-frame.core :as rf]
   [re-com.core :as re-com]
   [breaking-point.core :as bp]
   [spacefinder.components :as comp]
   ))

;; main
(defn main-panel []
)
